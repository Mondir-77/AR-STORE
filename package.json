{
  "name": "ar-store",
  "version": "1.0.0",
  "private": true,
  "description": "AR STORE — storefront + admin + API",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm install --prefix server",
    "start": "npm run start:render --prefix server"
  }
}
