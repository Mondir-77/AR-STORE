{
  "name": "ar-store",
  "version": "1.0.0",
  "private": true,
  "description": "AR STORE — storefront + admin + API",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm install --prefix server && npm run build --prefix server && npm run db:seed --prefix server",
    "start": "npm start --prefix server"
  }
}
